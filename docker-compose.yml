version: "3.3"

services:
  trading-bot:
    build: .
    container_name: krw-usdt-trading-bot
    ports:
      - "8000:8000"
    volumes:
      - ./config.yml:/app/config.yml:ro
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - TZ=Asia/Seoul
    restart: unless-stopped
